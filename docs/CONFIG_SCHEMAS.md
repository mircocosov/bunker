# Config Schemas

Schemas live in `packages/shared/src/schemas/config.ts`:
- `ruleSetSchema`
- `stateMachineSchema`
- `cardDefinitionSchema`
- `uiConfigSchema`
- `featureFlagSchema`

All schemas include `schema_version: 1` for forward compatibility.
